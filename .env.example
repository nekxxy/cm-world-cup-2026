# Phase 1 needs no secrets — the app builds and runs keyless.
# Telegram identity arrives at runtime via the WebApp SDK (no env vars).
#
# Reserved for later phases:
# TELEGRAM_BOT_TOKEN=        # for server-side initData verification
# DATABASE_URL=             # shared game state (added later)
